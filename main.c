#include <stdio.h>
#include <stdlib.h>

void* read_from_file(char* file)
{
    
    return
}

int main() {
    
    char input_file[] = "input.txt";

    int size = sizeof(int *);
    printf("%d\n", size );

    return 0;
}