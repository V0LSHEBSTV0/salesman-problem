{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run C Program",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/main",
            "cwd": "${workspaceFolder}",
            "console": "integratedTerminal",
            "stopAtEntry": false,
            "MIMode": "gdb"
        }
    ]
}
